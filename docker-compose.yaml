services:
  pixeltype:
    container_name: pixeltype
    build:
      context: .
      dockerfile: Dockerfile
    image: pixeltype:latest
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped